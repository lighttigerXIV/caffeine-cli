[package]
name = "caffeine-cli"
description = "A caffeine cli app to keep the screen awake. (Linux Only)"
version = "0.0.1"
edition = "2021"

[dependencies]
clap = {version = "4.5.27", features = ["derive"]}
inquire = "0.7.5"
